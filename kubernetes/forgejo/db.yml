apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: forgejo
  name: forgejo-db
spec:
  revisionHistoryLimit: 3
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: forgejo-db
  template:
    metadata:
      labels:
        app: forgejo-db
    spec:
      serviceAccountName: forgejo-db
      containers:
        - name: postgres
          image: postgres:18@sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: "forgejo"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: forgejo-db-secrets
                  key: credentials.json
            - name: POSTGRES_DB
              value: "forgejo"
          volumeMounts:
            - mountPath: /var/lib/postgresql
              name: postgres-data
      volumes:
        - name: postgres-data
          hostPath:
            path: /data/forgejo/postgres
            type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: forgejo-db-svc
  namespace: forgejo
  labels:
    app: forgejo-db
spec:
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  selector:
    app: forgejo-db
  type: ClusterIP
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: forgejo-db-external-secret # external secret name
  namespace: forgejo
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: forgejo-db-secrets # secret that will be created
    creationPolicy: Owner
  data:
    - secretKey: credentials.json
      remoteRef:
        key: forgejo
        property: POSTGRESS_PASSWORD
        conversionStrategy: Default	
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
